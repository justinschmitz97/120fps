---
kind: milestone
status: approved
---

# M97-M106 map: field-test run 4 remediation

Coordination document for the ten milestones remediating the 2026-08-21 field test run 4
(`C:\Projekte\120fps-fieldtest\REPORT.md`, 72 findings across 20 repositories, 8 independently
verified root causes in `C:\Projekte\120fps-fieldtest\verify\V1..V8`). Kept after approval, like the
M76-M83 and M84-M96 maps, because the approved specs cite it; it existed to keep their boundaries from
overlapping while three lanes edited concurrently.

Evidence: `C:\Projekte\120fps-fieldtest\` — `REPORT.md` (verdict and corrections), `findings/<repo>.md`
(every finding with repro, observed, expected, ground truth), `profiles/<repo>.md`, `logs/`,
`verify/V1-V8.md` (root causes with file:line, repro, fix direction, falsifying evidence).

Baseline at authoring time: branch `feat/m97-m106-field-test-run4` from `de55179`, 231 unit test files
(3798 passed, 1 POSIX-only skip, 106 s with `--maxWorkers=4`), `tsc --noEmit` clean. Scope: every blocker, major and minor finding. Out of scope: `question`
findings, and the three claims verification overturned — calcom-F1 (by design, M82 names cal.com),
calcom-F6 (refuted: `--explain-props` creates no harness dir), dub-F2 (refuted as parity; both paths
equally silent).

## Goal

A user pointing `120fps` at a `.js` React component, a dual-block Vue SFC, or a compound component
with mutually exclusive prop combos gets the component's real props, the component's real verdict,
and a dry run that predicts the real run. A run that is killed leaves nothing behind. Every remedy
printed names a command, file or flag that exists in that repository.

## Ordering

| Wave | Milestones | Audience |
|---|---|---|
| 1 — correctness | M97, M98, M99, M103 | every user |
| 2 — reliability | M101, M106 | every user |
| 3 — honesty | M100, M102, M104, M105 | every user |

## Lanes and file ownership

Ownership is by **file**. A lane never edits another lane's file. Where a milestone needs a change in
another lane's file, the interface below names it; the owning lane implements it first (interfaces
are the first thing each lane does), the consuming lane reads it. Anything not listed here that a
lane discovers it needs from another lane's file is an **interface request** to the coordinator, not
an edit.

| Lane | Owns | Milestones |
|---|---|---|
| A | `src/harness.ts`, `src/cli.ts`, `src/preflight.ts`, `src/page-errors.ts`, `src/project-model.ts`, `src/discovery.ts`, `src/shims/` | M101, M102, M105 (+ interfaces I1, I2, I3a, I5, I6, I7, I9, I11) |
| B | `src/prop-gen.ts`, `src/prop-gen-values.ts`, `src/prop-presets.ts`, `src/vue-sfc.ts`, `src/measure.ts`, `src/metrics.ts`, `src/noise.ts` | M97, M98, M103 (+ interfaces I4, I8, I10) |
| C | `src/analyze.ts`, `src/report.ts`, `src/hints.ts`, `src/composition.ts`, `src/isolation.ts`, `src/explorer.ts`, `src/react-profiler.ts`, `src/ci-report.ts`, `src/budget.ts`, `src/compare.ts`, `src/observers.ts`, `src/stress-patterns.ts` | M99, M100, M104 (+ interfaces I3b, I12) |

`src/index.ts` (re-exports) may be appended to by any lane for its own new exports, one line each.
Tests: each lane adds `test/unit/<behavior>.test.ts` files (behavior-named, never milestone-named;
no `mNN` tokens in describe/it titles) and may extend existing test files that exercise its own
files. Fixtures under `fixtures/` are shared; add new fixture directories, never edit existing ones.

## Rules for every lane

- Spec before code: `specs/milestones/mNN-<slug>.md` (frontmatter `kind: milestone`, `status: draft`,
  `tests:` listing the enforcing test paths) written first, with MUST / MUST NOT / Verification
  copied from this map and refined, then failing tests, then implementation.
- Edit only. Never `Write` an existing source file. Re-read the region before each edit; other lanes
  edit neighbouring regions of the same tree concurrently (not the same files).
- Verification is three-fold, recorded verbatim in the spec: (1) the new unit tests pass and the
  lane's existing tests still pass; (2) `pnpm lint` shows no error in the lane's files; (3) the real
  repository repro from the finding, run from `/e/repositories/<repo>` against a fresh `pnpm build`,
  now shows the expected behaviour. If `pnpm build` fails on another lane's file, wait two minutes,
  rebuild once, then note it and continue with unit evidence.
- Never edit target repositories under `E:\repositories` (read only; remove any `120fps-report.json`
  or `.120fps-harness-*` a repro created).
- `status: approved` only after the coordinator's verifier has re-run the repro.

## Cross-lane interfaces, fixed up front

- **I1 (A, closes material-ui-F2).** `src/harness.ts:2050` `transformWithEsbuild(code, id, { loader: "jsx" })`
  gains `jsx: "automatic"` and `jsxImportSource` (the project tsconfig's `jsxImportSource` when set,
  else `"react"`). `.js` files written for the automatic runtime (no React import) and files that
  import React both compile.
- **I2 (A, closes element-plus-F1; C swaps one call).** New `assertRendererSupported(componentPath,
  projectRoot)` in `src/harness.ts`: when `rendererFor(path) === "react"`, `detectFramework(projectRoot)
  === "vue"` and react-dom is not declared, throw a message that says the file is a `.tsx`/`.jsx` in a
  Vue project, that 120fps mounts Vue components from `.vue` SFCs only, and that `--framework vue`
  cannot change that. Called at `harness.ts:2509` before `assertReactDomClient`; Lane C calls it at
  `analyze.ts:2016-2029` in the same order. Lane C also stops printing the
  "Warnings recorded before this failure:" header when the list is empty (`analyze.ts:2374-2376`).
- **I3 (closes element-plus-F2).** I3a (A): `src/cli.ts:1130-1133` forwards `framework: args.framework`
  to `explainProps`. I3b (C): `explainProps` options gain `framework?: "react" | "vue" | "vanilla" | "auto"`,
  passed to `resolveFramework` at `analyze.ts:1986` instead of the hardcoded `"auto"`, so
  `FRAMEWORK_FLAG_NO_MOUNT_EFFECT_WARNING` prints in both modes.
- **I4 (B produces, C consumes; M99).** The rerender pass in `src/measure.ts` drains page errors twice:
  once after the stable samples (`:1517`, combo `ci`'s own window) into `pageErrors`, once after the
  prop-change rerender toward `combos[ci+1]` (`:1546`) into a new
  `transitionPageErrors?: { toComboIndex: number; errors: PageErrorSummary }`. Own errors keep their
  current meaning. Lane C renders transition errors on row `ci` as a transition disclosure and
  excludes them from `renderHealth`, `harnessFault` and the verdict.
- **I5 (A produces, C consumes; M100).** `src/harness.ts` exports
  `collectStaticPreBuildWarnings(projectRoot, opts)` returning `{ warnings, viteConfig, externalDeps,
  styleTooling, nextModules }` — everything `buildAndServe` computes before starting a server that needs
  only the filesystem (`readViteConfigData` and its warnings `:2593-2596`, `scanExternalDeps` and its
  warnings `:2609-2619` incl. BROKEN_ALIAS / UNBUILT_WORKSPACE_SOURCE_ALIAS / TYPE_ONLY,
  `UNSUPPORTED_NEXT_MODULE` `:2642`, `resolveStyleTooling` `:2666`, `PROJECT_TRANSFORM_WARNING` inputs).
  Revised in review: `buildAndServe` and `explainProps` each *call* it once per invocation — the
  hand-across option (`opts.preBuild`) had no producer and was removed, since the two are different
  invocations and a shared result would only be stale. What the interface guarantees is that both
  paths run the same function and therefore emit the same warnings in the same order
  (`test/unit/static-prebuild-warnings.test.ts`). `PROJECT_TRANSFORM_WARNING`'s inputs stay in
  `analyze.ts`, which both paths already reach. No warning text changes.
- **I6 (A; M102, closes mantine-F1).** `discoverGlobalCss(projectRoot, { extraEntryFiles })`: files
  listed are walked for stylesheet imports exactly like the project entry (`entryStylesheetImports`
  `:768`). Lane C passes the resolved wrapper path from `resolveCssFiles` (`analyze.ts:3186`).
- **I7 (A produces, C consumes; M102, closes excalidraw-F2).** The generated harness entry exposes
  `window.__120fps.stylesheetMatchStats()` returning, per injected global stylesheet,
  `{ file, rules, matched }` — `matched` counts rules whose `selectorText` matches at least one element
  under `#root` (CSSOM `document.styleSheets`, try/catch per sheet, no parser). Lane C reads it once
  after the first healthy mount, stores `matchedRules` in `css.details[i]`, and warns when a
  stylesheet with `rules > 0` has `matched === 0`, naming the file and saying the render measured as
  if unstyled.
- **I8 (B produces, C consumes; M103, closes calcom-F2).** `PropSchema.defaultValue?: unknown` and
  `defaultSource?: "destructuring" | "withDefaults" | "defaultProps"`. `formatExplainProps` prints a
  `default` column when any prop carries one.
- **I9 (A; M103, closes chakra-ui-F2).** `detectComponentExport` never picks an export whose name ends in
  `Provider` while another component export exists in the same file; the pick order is documented in
  the function's comment and in M103.
- **I10 (B or C; M104, closes twenty-F3, dub-F1, dub-F7).** Wherever matrix cells are selected: the
  anchor cell is kept under every `--max-combos` value (`cli.ts:926` promises it), non-axis props hold
  their declared default or are absent (never a synthesized truthy value), and a prop the component's
  own source references (M86 Tier-0) is never excluded from the axes while inherited DOM attributes
  are included. Lane C owns `buildMatrixReport` (`report.ts:1421`); if selection lives in a Lane B
  file, Lane B implements these three MUSTs and Lane C adds the tests on the report side.
- **I11 (A; M104, closes commerce-F3, material-ui-F3).** `formatWallClock` (`cli.ts:208`) carries
  seconds into minutes (`3m 0s`, never `2m 60s`). `--help` (`cli.ts:867`) states both defaults: combo-mode
  scale probes `1,5,20,50` and curve-mode points `1,3,5,10,20,50` (`analyze.ts:1108`), and which one
  `--scale` overrides.
- **I12 (C; M105, closes primevue-F2).** A mount-phase abort whose page error names a Vue injection
  (`$primevue`, `$slots`, `inject()`, `app.use`) or a React provider reaches the same hint pipeline as
  a render failure (`hints.ts`), so the failure path prints the provider / `--wrap` / slot remedy
  instead of a bare stack.

---

# Wave 1 — correctness

## M97 — JavaScript components resolve like TypeScript ones

**Lane B** (+ I1 in Lane A). Closes: material-ui-F1, material-ui-F2. Root cause: `verify/V3-material-ui.md`.

### MUST
- A `.js`/`.jsx` component with a sibling `.d.ts` (or a `types`/`typings` entry resolving to one)
  takes its props from the declaration: resolve `./<stem>` through `ts.resolveModuleName` and read the
  exported component's type before falling back to the JS binding (`prop-gen.ts:788-796`).
- A bound props type whose only properties are React ambient attributes (`ref`, `key`, or empty) is
  treated as unresolved: the fallback chain continues, and when nothing better exists the schema is
  empty with a warning naming the binding and why (`UNTYPED_JS_COMPONENT_WARNING`), never `ref`,`key`
  presented as the contract.
- ADR 0002 is amended (append-only, new ADR 0004 with `supersedes:`): "untyped JS components: default
  props only" applies to JS without any declaration; JS with a sibling declaration is typed.

### MUST NOT
- Change TypeScript extraction results for `.ts`/`.tsx` entries (pin with existing fixtures).

### Verification
Fixture `fixtures/js-with-dts/` (Badge.js + Badge.d.ts mirroring MUI's `OverridableComponent` shape,
a plain `.js` with destructuring defaults, a `.js` without any declaration). Real repo:
`cd /e/repositories/material-ui && node /c/Projekte/120fps/dist/cli.js packages/mui-material/src/Badge/Badge.js --explain-props`
lists `badgeContent`, `color`, `max`, `variant`, `invisible`… (16 props), and
`packages/mui-material/src/Chip/Chip.js --samples 3 --max-combos 2 --explore-budget 20` reaches a report
(no `React is not defined`).

## M98 — Vue props extract from every SFC shape the compiler accepts

**Lane B** (+ I2, I3). Closes: nuxt-ui-F1 (blocker), primevue-F1 (blocker), element-plus-F3,
element-plus-F5, element-plus-F1 (via I2), element-plus-F2 (via I3). Root cause: `verify/V5`, `verify/V4`.

### MUST
- `parseSfcScript` (`vue-sfc.ts:83-101`) feeds both `descriptor.script` and `descriptor.scriptSetup`
  into the virtual module, script block first, so a props interface declared in `<script lang="ts">`
  resolves for `defineProps<T>()` in `<script setup>`.
- When a `defineProps` type argument resolves to an error type, the warning names the unresolved type
  and the file it was expected in (`prop-gen.ts:531` currently returns silently).
- `applyPropPresets` (`prop-presets.ts:178`) appends a schema (`provenance: "preset"`, `required: false`,
  type inferred from the preset value) for every preset key absent from extraction;
  `UNKNOWN_PRESET_PROPS_WARNING` fires only when extraction produced at least one schema.
- A `string | number` (and `number | string`) prop synthesizes a member and is typed `union`, with the
  same disclosure every other union gets (element-plus-F3).
- A component using `setup()` with a runtime `props:` object is described as "runtime props object"
  (Composition API), never as "Options API" (element-plus-F5).

### MUST NOT
- Re-enable extraction for Options-API `extends`/`mixins` props (still excluded by ADR 0002); the
  change is that the documented escape hatch now works for them.

### Verification
Fixtures: dual-block SFC (`<script lang="ts">` exporting the interface + `<script setup>`), dual-block
with an unresolvable import, Options-API component + `<stem>.props.tsx`. Real repos:
`nuxt-ui src/runtime/components/Badge.vue --explain-props` lists `as label color variant size square class ui`;
`primevue packages/primevue/src/badge/Badge.vue --explain-props` with a temporary `Badge.props.tsx`
(`export default { value: '2', severity: 'success', size: 'large' }`, deleted afterwards) lists those
three props with `provenance: preset`; `element-plus packages/components/tabs/src/tabs.tsx --explain-props`
prints the Vue-`.tsx` message, with and without `--framework vue`.

## M99 — A page error belongs to the render that threw it

**Lane C** (+ I4 in Lane B). Closes: radix-primitives-F1, base-ui-F1, chakra-ui-F3. Root cause: `verify/V1`.

### MUST
- Errors captured during the prop-change rerender toward `combos[ci+1]` are reported as a transition
  (`ci → ci+1`) on row `ci` and excluded from that combo's `renderHealth`, `harnessFault` and verdict.
  They remain in JSON (`combos[ci].transitionPageErrors`) and in the console row tag.
- `detectHarnessFault`'s contract branch (`analyze.ts:444-458`) requires the captured error text to
  evidence the contract prop or its mechanism (`asChild`, `Slot`, `Slottable`, `cloneElement`,
  `render`), the same bar the placeholder branch applies (`:462-472`); otherwise the combo fails on its
  own terms and the provider hint pipeline runs.
- The interaction pass is audited for the same window (state it in the spec: shares the bleed or not,
  with file:line).

### MUST NOT
- Drop any error from JSON. Move an error to a different combo's verdict.

### Verification
Unit: a fake page-error buffer with errors injected between the stable samples and the transition
rerender lands on `transitionPageErrors`, not `pageErrors`; a contract-provenance combo whose error
says `useContext returned undefined` is not a harness fault. Real repos: radix Label unbounded run —
combos whose successor has `asChild: true` carry a transition tag and no `[1 page error]`; chakra
`badge.tsx` bounded run — no `[harness fault: asChild]`, provider hint names `ChakraProvider`.

## M103 — The measured props are the component's own

**Lane B** (+ I8, I9). Closes: chakra-ui-F1, heroui-F3, dub-F7, chakra-ui-F2 (I9), heroui-F2,
calcom-F2 (I8), base-ui-F3. Evidence: `findings/chakra-ui.md`, `findings/heroui.md`, `findings/dub.md`,
`findings/calcom.md`, `findings/base-ui.md`.

### MUST
- The 32-prop measurement cap fills from the component's own declared props first: props declared
  in the component's own file or its local type aliases (`RecipeProps`, `VariantProps`,
  `tailwind-variants` unions, `BadgeProps` literal unions) outrank inherited DOM attributes and event
  handlers, and a literal-union prop outranks an unknown-typed one. Document the rank table in the spec
  and say why M86's Tier-0 (source-referenced names) missed `colorPalette`/`size`/`variant` on chakra and
  `color`/`placement`/`size`/`variant` on heroui.
- `findComponentPropsType` binds the props of the export actually measured: for heroui `badge.tsx`,
  `BadgeRoot` (line 67, `children` optional), not `BadgeAnchor` (line 33, `children` required).
- Literal-union props reached through a generic or an intersection are typed as unions with their
  values (heroui `color` must not be `unknown`).
- `detectScalingProps` (`prop-gen.ts:203`) does not auto-activate curve mode on numeric props whose name
  denotes a bound or step (`min`, `max`, `step`, `precision`, `decimalScale`, `tabIndex`, `zIndex`);
  the exclusion list is in the spec.
- `PropSchema.defaultValue` per I8.

### MUST NOT
- Change which props are *extracted*; only ranking, binding and typing.

### Verification
Fixtures per shape. Real repos: chakra `packages/react/src/components/badge/badge.tsx --explain-props`
shows `colorPalette`, `size`, `variant` in the first 32; heroui `badge.tsx --explain-props` shows
`BadgeRoot`, line 67, `color` as a union; dub `badge.tsx --matrix --samples 3 --max-combos 4` includes
`variant` as an axis; base-ui `NumberFieldRoot.tsx --explain-props` says curve mode would not activate.

# Wave 2 — reliability

## M101 — A killed run leaves nothing behind

**Lane A.** Closes: base-ui-F2, heroui-F4, excalidraw-F3, shadcn-ui leftover (and dub-F4). Root cause:
`verify/V2-harness-leftovers.md`.

### MUST
- `SIGINT`, `SIGTERM` and `SIGHUP` run `sweepActiveHarnessDirs()`, close the browser pool, and re-exit
  with `128 + signo` (`cli.ts:1541` area).
- Every harness dir carries a `.pid` marker; `sweepStaleHarnessDirs` removes dirs whose pid is dead
  regardless of age, and the age gate for live-pid dirs drops from 1 h to 10 min.
- The fatal-exit watchdog bounds the whole run: armed before `runOne`, re-armed per phase heartbeat,
  so a hang inside `analyze()` cannot hold the dir and child processes indefinitely (budget: the
  larger of `--explore-budget` + 10 min or 20 min; document it).
- Child processes (Chromium, esbuild) are killed on every exit path above.

### MUST NOT
- Sweep a dir whose pid is alive and younger than the gate. Touch anything outside `.120fps-harness-*`.

### Verification
Unit: pid-marker sweep with a dead pid removes, with the current pid keeps; signal handler registered
for all three signals. Real: start a zero-config base-ui Button run, `kill -INT` it after
`harness: building`, assert no `.120fps-harness-*` under `packages/react` and no orphan `node`/chrome
child within 5 s; repeat with `taskkill /F` and a following `--explain-props` run, assert the dead-pid
sweep removed it.

## M106 — Composition, portals and crashes found only on the real corpus

**Investigation first (read-only worker), fixes by owning lane.** Closes: calcom-F3
(`Tracing.tracingComplete timed out` in explore on Radix Popover/Portal), excalidraw-F1 (TDZ
`Cannot access 'DropdownMenu' before initialization`), twenty-F2 (sass `additionalData` globals not
replicated; `vite.config.ts` is text-parsed), dub-F6 (Combobox renders through a Portal, `domNodeCount`
0, no portal disclosure), calcom-F5 (`<use href>` sprite reference renders empty, no disclosure).

### MUST
Root causes are established (`verify/M106-investigation.md`); the binding MUSTs live in
`specs/milestones/m106-corpus-crashes-and-empty-renders.md`, split per lane (B1-B5, C1-C4, A1-A4). Two
of the draft MUSTs were restated there: dub-F6 is a render error curve mode never surfaced (portal
content was already counted), and excalidraw-F1 is handled by `import-cycle` detection in preflight,
not by reordering the entry graph. Lane B's B3+B4+B5 are one handoff to Lane C.

# Wave 3 — honesty

## M100 — The dry run and the real run share one static diagnosis

**Lane C** (+ I5 in Lane A). Closes: twenty-F1, dub-F3, calcom-F4, element-plus-F4, chakra-ui-F4,
nuxt-ui-F3, preact-app-F1, excalidraw-F4. Rescopes: taxonomy-F1, mantine-F2 (runtime-only). Root
cause: `verify/V6-explain-props-parity.md`.

### MUST
- `explainProps` and `analyze()` both call `collectStaticPreBuildWarnings` (I5) and print its
  warnings identically; the `Stylesheets:` line (`analyze.ts:2533`) and the alternative-export note
  (`:2084`) print in both modes.
- Mode prediction in the dry run uses the real dispatcher's predicate and precedence
  (`:3067-3099`) through one shared `predictMode()`; "Matrix mode: would auto-activate" is printed only
  when the real run would pick matrix.
- A composed run that skips `getSchemas()` (`analyze.ts:1605`, `:944`) still emits the extraction
  warnings the dry run printed, plus a "measured with no props" caveat when `props` is `{}`.
- M91's MUST NOT is restated in M100 as: *a refusal or warning decidable from the filesystem alone
  appears in both modes*; refusals that need the browser (module evaluation, provider throws) are
  listed as runtime-only, and the dry run's footer says so in one line.

### MUST NOT
- Start a Vite server or browser from `--explain-props`.

### Verification
Unit: for each warning source in V6's table, a test asserting both paths emit it. Real repos: dub
`badge.tsx` dry-run warnings ⊇ the real run's pre-build warnings (diff of the two `warnings` sets is
empty on the static side); preact-app `components/button.tsx` both modes print the `Stylesheets:` line;
element-plus `badge.vue --explain-props` predicts curve, matching the real run.

## M102 — Stylesheet discovery reads the package's own declarations and discloses what applied

**Lane A** (+ I6, I7 consumed by C). Closes: heroui-F1, mantine-F1, shadcn-ui-F1, shadcn-ui-F2,
shadcn-ui-F3, excalidraw-F2. Not closed (by design, M82): calcom-F1. Root cause: `verify/V8`.

### MUST
- The measured package's own `package.json` `style`, `exports["./styles"]`, `exports["./style.css"]`
  and `exports[*].style` are stylesheet candidates ranked above the largest-file fallback; a candidate
  that is a 0-rule passthrough has its `@import` targets resolved one hop (bare specifiers through
  `resolveBareStylesheetSpecifier`), and the resolved files become the candidates.
- `GLOBAL_CSS_CANDIDATES` includes `src/styles.css` next to `src/style.css`.
- A stylesheet read failure names the path that was actually tried (the resolved real path, e.g.
  `packages/shadcn/dist/tailwind.css`), never a path assembled from the specifier that does not exist.
- The same unreadable-stylesheet condition has one outcome: warn, drop, measure unstyled. The fatal
  path select.tsx/sidebar.tsx hit is traced (state the surface in the spec) and routed to the same
  warning.
- `css.details` is populated whenever `layer` is set (`unreadable` entries carry the file and the
  reason), and per I7 carries `matchedRules`.

### MUST NOT
- Consult an ancestor application's entry for a library package (M82).

### Verification
Real repos: heroui `badge.tsx` bounded run → `Stylesheets:` names `@heroui/styles/index.css` (resolved
via `exports["./styles"]` → `src/styles.css` → `@import`); mantine `Badge.tsx` with a wrapper → the
three MantineProvider stylesheets are disclosed; shadcn `select.tsx` default run reaches a report with
the unstyled warning naming `packages/shadcn/dist/tailwind.css`; excalidraw `FilledButton.tsx` → warning
that `css/styles.scss` matched 0 of N rules.

## M104 — Modes measure what they say they measure

**Lane C** (+ I10, I11). Closes: twenty-F3, dub-F1, dub-F5, dub-F7 (with M103), commerce-F1,
commerce-F2, commerce-F3 (I11), material-ui-F3 (I11).

### MUST
- Matrix: anchor kept under every `--max-combos` (I10); non-axis props at default/absent (dub Switch's
  `disabledTooltip` is not held truthy); the report header says which axes were kept and which dropped.
- `combos[]` in JSON holds prop combos only; scale probes move to `scaleProbes[]` (or carry
  `scaleProbe: true` and are excluded from every "N of M combos" count); the footer and the
  `--max-combos` warning agree.
- Render attribution prints in curve and matrix modes whenever a React profiler snapshot exists, with
  the same section as combo mode.
- A curve point with `domNodeCount === 0` is tagged `renders nothing at N=…` and excluded from the fit
  (disclosed), never fitted as an ordinary point.

### Verification
Unit per MUST. Real repos: twenty `Modal.tsx --matrix --max-combos 2` keeps `isOpen=true`; dub
`switch.tsx --matrix` passes without a TooltipProvider crash; dub `badge.tsx` footer counts agree;
commerce `gallery.tsx` curve run prints Render attribution; commerce `variant-selector.tsx` N=1 row is
tagged; `Total: 3m 0s` formatting.

## M105 — Every remedy names something that exists

**Lane A** (+ I12 in Lane C). Closes: ant-design-F1, nuxt-ui-F2, solid-ui-F1, pnp-app-F1,
chakra-ui-F6, taxonomy-F3, primevue-F2 (I12).

### MUST
- A missing generated file remedy names the `package.json` script whose command references the
  generator or the missing path (ant-design: `npm run version`, `scripts/generate-version.ts`), falling
  back to the build script only when no such script exists; the command uses the detected package
  manager (`pnpm run build` in a pnpm repo, `yarn build` under yarn) — nuxt-ui.
- `--no-preflight` failure text never advises passing `--no-preflight` when it was passed.
- `PREFLIGHT_BYPASSED_WARNING` labels each bypassed kind by its own name (`yarn-pnp`, `solid`,
  `server-boundary`), per `preflight.ts:660`.
- The alias-source warning states when the aliased package's `main`/`exports` point at an absent
  `dist/` (chakra-ui-F6).
- `waitForReadyOrFatal` (`page-errors.ts:284-299`) delivers the fatal signal's remedy when the fatal
  signal arrives before or within the ready timeout: `enrichTimeoutError` accepts the remedy line, so
  `NO_ENV_FILE_REMEDY_NOTE` prints for taxonomy's env-validation throw.

### Verification
Real repos: ant-design `components/button/Button.tsx` default run names `npm run version`; nuxt-ui
names `pnpm run build` (or `pnpm nuxt prepare`); solid-ui with `--no-preflight` no longer suggests it;
pnp-app `--no-preflight` summary says `yarn-pnp`; taxonomy `badge.tsx` prints the env remedy note.

---

## Closing

When all ten specs are `approved`: update `specs/overview/02-milestones.md` (one entry per milestone:
About / Why this way / Achievement), `specs/overview/00-tdd.md:51` suite count, glossary terms
(transition error, preset-added prop, stylesheet match stats), then delete this map.
